{%- comment -%}
Section: Product — Use Cases
Purpose: Show practical applications with card-based visual hierarchy
{%- endcomment -%}

{{ 'zack-modules.css' | asset_url | stylesheet_tag }}

<section id="z-use-cases" class="z-section" data-section-id="{{ section.id }}">
  <div class="z-container">
    <h2>{{ section.settings.title | default: 'How Families Use Zack AI' }}</h2>
    <div class="z-grid">
      {%- for block in section.blocks -%}
        {%- if block.type == 'use_case' -%}
          <article class="z-card" {{ block.shopify_attributes }}>
            <h3>{% if block.settings.icon != blank %}<span aria-hidden="true">{{ block.settings.icon }}</span> {% endif %}{{ block.settings.heading }}</h3>
            <p>{{ block.settings.text }}</p>
          </article>
        {%- endif -%}
      {%- endfor -%}
    </div>
    {%- if section.settings.cta_text != blank and section.settings.cta_link != blank -%}
      <div style="margin-top:16px">
        <a href="{{ section.settings.cta_link }}" class="z-btn z-btn--ghost">{{ section.settings.cta_text }}</a>
      </div>
    {%- endif -%}
  </div>
</section>

{% schema %}
{
  "name": "Product — Use Cases",
  "tag": "section",
  "class": "section",
  "settings": [
    {"type":"text","id":"title","label":"Section title","default":"How Families Use Zack AI"},
    {"type":"text","id":"cta_text","label":"Optional CTA text","default":"See Learning Outcomes"},
    {"type":"url","id":"cta_link","label":"Optional CTA link","default":"#z-tech-specs"}
  ],
  "blocks": [
    {
      "type":"use_case",
      "name":"Use case card",
      "limit": 8,
      "settings": [
        {"type":"text","id":"icon","label":"Emoji/Icon (optional)","default":"🏠"},
        {"type":"text","id":"heading","label":"Heading"},
        {"type":"textarea","id":"text","label":"Description"}
      ]
    }
  ],
  "presets": [
    {
      "name": "Product — Use Cases",
      "category": "Product",
      "blocks": [
        {
          "type": "use_case",
          "settings": {
            "icon": "🏠",
            "heading": "Indoor family use",
            "text": "Screen‑free conversations at the dinner table, storytime, and homework check‑ins."
          }
        },
        {
          "type": "use_case",
          "settings": {
            "icon": "🎁",
            "heading": "Gift‑giving occasions",
            "text": "A premium, meaningful present for birthdays and holidays that grows with children."
          }
        },
        {
          "type": "use_case",
          "settings": {
            "icon": "📚",
            "heading": "Learning & play‑based engagement",
            "text": "Language practice, educational games, bedtime stories, and empathy‑building interactions."
          }
        }
      ]
    }
  ]
}
{% endschema %}
