# Crowdfunding Template Processing Automation System
# Core dependencies for template processing and brand validation

# YAML processing for brand configuration
PyYAML>=6.0.1

# CLI argument parsing (built-in argparse is sufficient)
# JSON handling (built-in json is sufficient)
# Logging (built-in logging is sufficient)
# Path handling (built-in pathlib is sufficient)
# Regular expressions (built-in re is sufficient)
# Date/time handling (built-in datetime is sufficient)

# Optional: Future OpenRouter API integration (Phase 2)
# requests>=2.31.0
# openai>=1.0.0

# Development and testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Documentation
sphinx>=7.1.0
sphinx-rtd-theme>=1.3.0